spring.application.name=TEACHER-SERVICE

server.port = 3538

# database credentials we are providing to talk with spring data jpa, jakarta persistence access

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url = jdbc:mysql://localhost:3306/teacherService
spring.datasource.username = root
spring.datasource.password = @Ayush1225
spring.jpa.show-sql = true
spring.jpa.hibernate.ddl-auto = update
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL8Dialect

#Registry on netflix eureka spring cloud for handling microservices

eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true
eureka.client.service-url.defaultZone=http://localhost:3535/eureka/
eureka.instance.hostname=localhost